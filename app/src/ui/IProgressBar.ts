export interface IProgressBarProps {
    buffer?: number,
    accent?: boolean,
    children?: any,
    className?: string,
    disabled?: boolean,
    flat?: boolean,
    floating?: boolean,
    icon?: any,
    mode?: string,
    multicolor?: boolean,
    min?: number,
    max?: number,
    mini?: boolean,
    neutral?: boolean,
    type?: string,
    value?: number
};
